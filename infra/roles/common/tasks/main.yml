- name: Install deps
  apt:
    name: [curl, apt-transport-https]
    update_cache: yes

- name: Install python3-pip
  apt:
    name: python3-pip
    state: present
    update_cache: yes

- name: Install kubernetes Python library
  pip:
    name: kubernetes
    executable: pip3